import React from 'react'

const Boxing = () => {
    return (
        <div>index</div>
    )
}

export default Boxing;
// instead of doing this as individual pages for each sport, why not just make a single page and pass in the sport as a prop?
// and then fill in the page with the appropriate data based on the prop passed in?